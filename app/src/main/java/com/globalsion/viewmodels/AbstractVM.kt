package com.globalsion.viewmodels

import android.app.Application
import android.arch.lifecycle.AndroidViewModel

abstract class AbstractVM(application: Application) : AndroidViewModel(application) {
}